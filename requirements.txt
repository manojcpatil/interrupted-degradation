# Dependencies for the replication code.
#
# The published results were produced under Python 3.12.2 (CPython) with the
# versions pinned below; the full environment stamp is in
# Output/environment.json. Later versions are expected to work — no result
# depends on execution timing or on version-specific numerical behaviour — but
# these are the versions the reported numbers were computed with.

numpy==1.26.4
sympy==1.13.2
scipy==1.14.0
matplotlib==3.11.0
